# Simple CMake scripts

Start by running the following command to see what happens.

```bash
cmake -P hello.cmake
```

Take a look at the file to see the commands. Then inspect the `more_commands.cmake` file and run it to see the output.